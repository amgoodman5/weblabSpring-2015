/**
 * Created by AaronG on 3/30/2015.
 */
var ttt=['x','o','x','','','x', 'o','','x']

var lookups={//define matches
];
lookups.forEach(function(alookup){
    if(checkForWin(ttt,aLookup))console.log(aLookup); //passes through all the results)

    function checkforwin(board,match){
    if(board[match[0]]
        && board[match[0]]====board [march[1]]
        && board[match[1]]===board[march[2]]){
        return true;
    }
    return false;

        //function forEach(array,callback)
        for(var i=0; i < aar)
}
